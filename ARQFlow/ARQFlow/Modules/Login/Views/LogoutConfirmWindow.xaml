<Window x:Class="ARQFlow.App.LogoutConfirmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirmar logout" Height="150" Width="360"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Para confirmar o logout, digite LOGOUT no campo abaixo:" TextWrapping="Wrap" />
        <TextBox x:Name="txtConfirm" Grid.Row="1" Margin="0,8,0,8" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Confirmar" Width="90" Margin="0,0,8,0" Click="BtnConfirmar_Click" />
            <Button Content="Cancelar" Width="90" Click="BtnCancelar_Click" />
        </StackPanel>
    </Grid>
</Window>
