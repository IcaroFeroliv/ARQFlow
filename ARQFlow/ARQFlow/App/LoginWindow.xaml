<Window x:Class="ARQFlow.App.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ARQ Flow - Login" Height="220" Width="360"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Email" VerticalAlignment="Center" Margin="0,0,12,12" />
        <TextBox x:Name="txtEmail" Grid.Column="1" Margin="0,0,0,12" />

        <TextBlock Text="Senha" Grid.Row="1" VerticalAlignment="Center" Margin="0,0,12,12" />
        <PasswordBox x:Name="txtSenha" Grid.Row="1" Grid.Column="1" Margin="0,0,0,12" />

        <TextBlock x:Name="txtStatus" Grid.Row="2" Grid.ColumnSpan="2" Foreground="DarkRed" Margin="0,0,0,12" TextWrapping="Wrap" />

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Entrar" Width="90" Margin="0,0,8,0" Click="BtnEntrar_Click" />
            <Button Content="Cancelar" Width="90" Click="BtnCancelar_Click" />
        </StackPanel>
    </Grid>
</Window>
